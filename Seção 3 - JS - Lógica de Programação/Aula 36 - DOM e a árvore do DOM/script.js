// DOM = Document Object Model

/**
 * Window = primeiro, Janela
 * 
 * DOM é o que permite a manipulação dos elementos, por exemplo:
 * (elemento, suponhamos um container).querySelector('h1');
 * Está puxando dentro do elemento do container, um child com tag h1
 */